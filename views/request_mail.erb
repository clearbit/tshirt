Hi there!

Thanks for requesting a Clearbit T-shirt!

<% if request.name? && request.address? %>
Is this the correct information?

<%= request.name %>,
<%= request.street_one %>,
<%= request.street_two %>,
<%= request.city %>,
<%= request.state %> <%= request.zip %>

<a href="http://tshirt.clearbit.io/requests/<%= request.id %>/confirm">Yes</a> | <a href="http://tshirt.clearbit.io/requests/<%= request.id %>">Nope, not even close</a>
<% else %>
We just need to <a href="http://tshirt.clearbit.io/requests/<%= request.id %>">confirm some address data</a>.
<% end %>

Cheers,
Clearbit TShirt Bot
